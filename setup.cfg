[metadata]
name = loan-prediction-app
version = 0.1.0

[options]
packages = find:
python_requires = >=3.11

[tool:pytest]
addopts = -ra -q
testpaths =
    backend/tests
    frontend/tests

[coverage:run]
branch = True
source =
    backend
    frontend
